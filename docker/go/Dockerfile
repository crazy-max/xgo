ARG GO_VERSION
ARG GO_DIST_URL
ARG GO_DIST_SHA
ARG BASE_IMAGE=ghcr.io/crazy-max/xgo:base
FROM ${BASE_IMAGE}

ARG GO_VERSION
ARG GO_DIST_URL
ARG GO_DIST_SHA
ENV GO_VERSION=${GO_VERSION}
RUN $BOOTSTRAP_PURE
